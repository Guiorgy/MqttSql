{
	// Minimal configuration without arrays. The database, host, port, TLS, QOS and timestamp format are all set to the default values.
	"broker": {
		"user": "user",
		"password": "password",
		"subscription": {
			"topic": "some/topic",
			"table": "table"
		}
	}
}