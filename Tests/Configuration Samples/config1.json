{
	// Minimal configuration. The database, host, port and QOS are all set to the default values.
	"brokers": [
		{
			"user": "user",
			"password": "password",
			"subscriptions": [
				{
					"topic": "some/topic",
					"table": "table"
				}
			]
		}
	]
}